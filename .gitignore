# Offline geometry utilities — separate repo, not part of the web app
geometry_simplifier/

# QGIS layer metadata (not needed by web app)
*.qmd

# Claude Code files
.claude/
CLAUDE.md

# OS noise
.DS_Store
Thumbs.db
